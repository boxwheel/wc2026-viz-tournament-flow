Manual flywheel upload flow (kept as reference):
1. mcp commit_new_node -> node_id, revision=0
2. mcp prepare_artifact_uploads -> upload_url, batch_token
3. curl PUT @file -> 202
4. mcp finalize_artifact_uploads -> revision bump
5. mcp set_sharing_for_node public
6. mcp set_node_tag_assignments expected_revision=current
